version: '3.3'

services:
    consul:
        build: Consul
        volumes:
            - /home/pi/Adapter/AML:/AML
    parser:
        build: Parser
        volumes:
            - /home/pi/Adapter/AML:/AML
    minio:
        build: Minio
        volumes:
            - /home/pi/Adapter/AML:/AML

    